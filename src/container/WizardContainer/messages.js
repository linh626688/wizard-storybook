/*
 * WizardContainer Messages
 *
 * This contains all the text for the WizardContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WizardContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WizardContainer container!',
  },
});
